import { LastNameConverterPipe } from './last-name-converter.pipe';

describe('LastNameConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new LastNameConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
